#!/bin/bash
set -e -E -u

BASE_DIR=$(dirname $0)/..
SRC_DIR=$BASE_DIR/src
TEMPLATES_DIR=$BASE_DIR/templates

kubectl create configmap auto-updater-scripts \
    --from-file $SRC_DIR/sync-images \
    --from-file $SRC_DIR/sync-pods \
    --dry-run -o yaml > $TEMPLATES_DIR/scripts.yaml
